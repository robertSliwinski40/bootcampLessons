package Streams;
/*
streams work with collections (array, set, list, map)
stream(),
filter, map, reduce-intermediate operators
sum(), collect(c.toList()), average(), collect(c.groupingBy()) - terminal operators

 */
public class Main {

}
