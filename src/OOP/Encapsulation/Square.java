package OOP.Encapsulation;

//Encapsulation - we are bundling the state, properties or data and the methods
// ,the behaviours together into one single unit

public class Square {
    int length;

    int area(){
        return length*length;
    }
}
